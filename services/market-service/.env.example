# Path to the market dataset (relative to the service root).
MARKET_DATA_PATH=data/market_data.parquet

WEB_ORIGIN=http://localhost:3000

# Redis Cloud connection string (placeholder; set a real value in .env).
REDIS_URL=redis://localhost:6379/0
REDIS_CACHE_TTL_SECONDS=600